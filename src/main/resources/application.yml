spring:
  application:
    name: studying
  datasource:
    url: jdbc:postgresql://db:5432/ct
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto:  update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

custom:
  parameter: Hello world